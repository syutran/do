<h3>Listing categories</h3>

<% @categories.arrange_as_array.each do |category| %>
  <div class="span8 offset<%= category.depth%>">
    <i icon="icon-user"><strong> <%= category.title %></strong>
  </div>
<% end %>

<%= link_to 'New Category', new_category_path %>
